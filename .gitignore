# dependencies
node_modules/

# build output
dist/
dist-build/

# env — never commit secrets
.env
.env.local
.env.*.local

# vite / editor caches
.vite-cache/
.tmp-vv-method/
.claude/
.cursor/

# netlify build cache/state only — keep netlify/functions source committed
.netlify/

# OS
Thumbs.db
.DS_Store
